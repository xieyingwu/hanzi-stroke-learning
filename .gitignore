# 笔顺数据文件（本地离线用，体积过大不纳入版本控制）
# 运行 `npm pack hanzi-writer-data@2.0` 并解压到此目录可离线使用
hanzi-data/

# 构建生成的笔顺分片（约数 MB，CI/本地 build 时生成；亦可提交以跳过构建步骤）
stroke-data/

node_modules/
dist/

# 沙盒/本机便携 Node（解压自 nodejs.org，勿提交）
node-v*-darwin-*/

# npm pack 等本地压缩包（勿提交）
*.tgz

# 系统文件
.DS_Store
Thumbs.db
